DB_CONNECTION_STRING=postgresql://username:password@host:port/dbname?sslmode=require

POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres  # Change for prod
POSTGRES_DB=floor_heights
POSTGRES_PORT=5432


AWS_ACCESS_KEY_ID=your-access-key-here
AWS_SECRET_ACCESS_KEY=your-secret-access-key-here
AWS_DEFAULT_REGION=ap-southeast-2

# Docker user mapping (to avoid permission issues with mounted volumes)
UID=1000  # Your host user ID, run 'id -u' to get it
GID=1000  # Your host group ID, run 'id -g' to get it

# Machine-specific paths (if different from config files)
# OUTPUT_ROOT_OVERRIDE=/path/to/output  # Uncomment to override config/common.yaml